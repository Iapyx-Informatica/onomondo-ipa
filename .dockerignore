# Keep the Docker build context lean.
# build/ is an artifact directory; nvstate.bin is runtime state.
# Note: libasn/*.{c,h} IS kept in the context so the directory exists for
# regenerate_libasn.sh to cd into; the script wipes it before regenerating.
build/
**/nvstate.bin
**/*.o
**/*.obj
.git/
*.zip
